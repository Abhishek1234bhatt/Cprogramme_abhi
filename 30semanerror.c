#include <stdio.h>

int main() {
    int a = 10, b = 20;
    int sum

    // This will cause a syntax error due to missing semicolon
    sum = a + b;
    printf("Sum: %d\n", sum);

    return 0;
}
